export default class User {
    constructor(username, show_email, bio, password) {
      this.username = username;
      this.show_email = show_email;
      this.bio = bio;
      this.password = password;
    }
  }